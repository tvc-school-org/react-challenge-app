// Logo
import React from 'react';
import { Table } from 'react-bootstrap';
import './style.css';

const Logo = (props) => (
  <Table>
    <thead>
      props.hableHeadings.map((t, index) = {
        <td>t</td>
      }
    </thead>
  </Table>

)

export default Logo;
